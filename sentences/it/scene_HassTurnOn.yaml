language: it
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<turn_on> [<scene>] <name>"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
      - sentences:
          - "<turn_on> [<scene>] <name> [(<of>|<in>)] <area>"
        requires_context:
          domain: scene
        slots:
          domain: scene
        response: scene
